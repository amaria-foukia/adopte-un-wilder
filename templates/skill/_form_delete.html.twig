<form method="post" action="{{ path('skill_delete', {'id': skill.id}) }}" onsubmit="return confirm('Supprimer cette compétence?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.id) }}">
    <button class="btn btn-primary px-3"><i class="far fa-trash-alt"></i></button>
</form>
