<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top d-print-none">
    <div class="container">
        <a class="navbar-brand" href="/"><img id="logo-navbar" src="/assets/images/adopte-un-wilder-logo-blanc-removebg-preview.png" alt="logo-adopte-un-wilder-blanc"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('wilder_index') }}">Nos Wilders</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('about') }}">A propos</a>
                </li>
            </ul>
            {% if app.user %}
                <ul class="navbar-nav">
                    <li class="nav-item dropdown" style="transform: scale(1)">
                        <a class="nav-link dropdown-toggle text-white" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><i class="fas fa-user"></i><span>&nbsp;{{ app.user.firstname }}</span></a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('account') }}"><i class="fas fa-tools"></i>&nbsp;Dashboard</a>
                            <a class="dropdown-item" href="{{ path('wilder_show', {slug: app.user.slug}) }}"><i class="fas fa-id-card"></i>&nbsp;Mon CV</a>
                            {% if app.user.roles[1] is defined and app.user.roles[1] == 'ROLE_ADMIN' %}
                                <a class="dropdown-item" href="{{ path('admin') }}"><i class="fas fa-user-cog"></i>&nbsp;Administration</a>
                            {% endif %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('logout') }}"><i class="fas fa-sign-out-alt"></i>&nbsp;Deconnexion</a>
                        </div>
                    </li>
                </ul>
            {% else %}
                <a class="text-white text-decoration-none" href="{{ path('login') }}">Connexion &nbsp; | &nbsp;</a>
                <a class="text-white text-decoration-none" href="{{ path('register') }}"> Inscription</a>
            {% endif %}
        </div>
    </div>
</nav>
