<form method="post" action="{{ path('education_delete', {'id': education.id}) }}" onsubmit="return confirm('Supprimer cette formation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ education.id) }}">
    <button class="btn btn-primary px-3"><i class="far fa-trash-alt"></i></button>
</form>
